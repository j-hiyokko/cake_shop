<h2>items#index</h2>

<h2>商品一覧(全<%= @items.count %>件)</h2>
<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
  <%= image_tag item.get_item_image,size: "300x250" %>
  <% end %>
  <%= item.item_name %>
  ¥<%= item.price %>
<% end %>
