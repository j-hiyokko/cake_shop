<table class="table table-bordered">
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% @genre.each do |genre| %>
    <tr>
      <td>
        <%= link_to genre.genre_name, genre_path(genre.id) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
