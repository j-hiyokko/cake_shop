<div class="container mt-3">

  <div class="row">

    <div class="col-md-2 mt-5">
      <%= render  "public/items/sidebar", genres: @genres %>
    </div>

    <div class="col-md-7 offset-md-1">
      <div class="card">
        <div class="card-img">
          <%= image_tag'cookie.jpg' %>
          <div class="card-img-overlay">
            <p class="text-white px-2 py-4">
              ようこそ、ながのCAKEへ！<br>
              このサイトは、ケーキ販売のECサイトになります。<br>
              会員ではない方も商品の閲覧はできますが、<br>
              購入には会員登録が必要になります。
            </p>
          </div>
        </div>
      </div>
    </div>

  </div>

  <div class="row mt-3">
      <div class='col-md-8 offset-md-4'>
        <h4>新着商品</h4>
          <div class="row">
            <% @items.each do |item| %>
              <div class='col-md-2'>
                <%= link_to item_path(item.id) do %>
                <%= image_tag item.get_item_image,size: "100x80" %>
                <% end %>
                <%= item.item_name %><br>
                ¥<%= item.add_tax_price.to_s(:delimited) %>
              </div>
            <% end %>
          </div>
        </div>

      <div class="col-md-10 offset-9 mb-4">
        <%= link_to "すべての商品を見る", items_path, class:"btn btn-outline-info btn-sm" %>
      </div>
  </div>
