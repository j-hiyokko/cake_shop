<div class="container">

  <div class="row py-5">

    <div class="col-md-2">
      <%= render  "public/items/sidebar", genres: @genres %>
    </div>

    <div class="col-md-7 offset-md-1">
      <div class="card">
        <div class="card-img">
          <%= image_tag'cookie.jpg' %>
          <div class="card-img-overlay">
            <p class="font-weight-bold text-white px-2 py-2" style="line-height:3rem">
              ようこそ、ながのCAKEへ！<br>
              このサイトは、ケーキ販売のECサイトになります。<br>
              会員ではない方も商品の閲覧はできますが、<br>
              購入には会員登録が必要になります。
            </p>
          </div>
        </div>
      </div>
    </div>

  </div>

  <div class="row mt-3">
      <div class='col-md-8 offset-md-3'>
        <h4>新着商品</h4>
          <div class="row mt-3">
            <% @items.each do |item| %>
              <div class='col'>
                <%= link_to item_path(item.id) do %>
                <%= image_tag item.get_item_image,size: "150x120",class:"img-thumbnail" %>
                <% end %><br>
                <%= item.item_name %><br>
                ¥<%= item.add_tax_price.to_s(:delimited) %>
              </div>
            <% end %>
          </div>

          <div class="text-right mt-3">
            <%= link_to "すべての商品を見る", items_path, class:"btn btn-outline-info btn-sm text-right" %>
          </div>
      </div>
  </div>

</div>
